The project was developed in Eclipse Parallel Tool Platform, but you should be able to import it in any IDE of preference.

Remember to enable OpenMP during compilation, by enabling the proper flag to automatically link to OpenMP runtime.

e.g. for GCC you may use -fopenmp